# lambda is used to create one line function , which means short form of functions , used to reduce no. of line
# def func(args):
#    return ret_val
# can be written as
# func = lambda args: ret_val
m = lambda a, b, c : a + b
print(m(1,2,3))
